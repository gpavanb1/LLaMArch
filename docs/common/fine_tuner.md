# FineTuner

The `FineTuner` class is a component in the `llamarch` library that provides a simple interface to work with fine-tuning models.

> Note: Fine-tuning is only supported for Hugging Face models in this implementation.

::: llamarch.common.fine_tuner