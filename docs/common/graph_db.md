# GraphDB

The `GraphDB` class is a component in the `llamarch` library that provides a simple interface to work with graph databases, particularly [Neo4j](https://neo4j.com/). It allows for the creation, retrieval, and deletion of nodes and relationships in the graph.

::: llamarch.common.graph_db