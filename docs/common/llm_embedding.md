# LLM Embedding

The `LLMEmbedding` class is a component in the `llamarch` library that provides a simple interface to work with LLM embeddings. These provide a way to embed text into a vector space, which can be useful for various applications.

::: llamarch.common.llm_embedding