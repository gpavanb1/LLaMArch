# VectorDB

The `VectorDB` class is a component in the `llamarch` library that provides a simple interface to work with vector databases. It allows for the creation, retrieval, and deletion of vectors in the database. Currrently, only the [Qdrant](https://qdrant.tech/) vector database is supported.

::: llamarch.common.vector_db