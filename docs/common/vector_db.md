# VectorDB

The `VectorDB` class is a component in the `llamarch` library that provides a simple interface to work with vector databases. It allows for the creation, retrieval, and deletion of vectors in the database. Currently, multiple vector databases are supported, such as Pinecone, Weaviate, Qdrant, and Chroma.

::: llamarch.common.vector_db