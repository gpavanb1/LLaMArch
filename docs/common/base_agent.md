# Base Agent

The `BaseAgent` class is a foundational component in the `llamarch` library. It provides a basic structure for agents that can interact with LLMs and provides basic performance updates.

::: llamarch.common.base_agent