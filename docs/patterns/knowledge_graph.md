# Knowledge Graph

![img](../images/patterns/knowledge_graph.jpg)

The `KnowledgeGraph` class is a component in the `llamarch` library that provides a simple interface to work with knowledge graphs.

::: llamarch.patterns.knowledge_graph